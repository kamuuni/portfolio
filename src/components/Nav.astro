---
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-sm border-b border-stone-200">
  <div class="max-w-6xl mx-auto px-6 md:px-12 lg:px-24 h-16 flex items-center justify-between">
    <!-- Logo: leaf + name -->
    <a href="#hero" class="flex items-center gap-2 group">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" class="text-emerald-600 group-hover:text-emerald-500 transition-colors">
        <path d="M12 22C12 22 3 16 3 9a9 9 0 0 1 18 0c0 7-9 13-9 13z" fill="currentColor" opacity="0.2" stroke="currentColor" stroke-width="1.5"/>
        <path d="M12 22V9M9 12l3-3 3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span class="font-bold text-sm tracking-wide" style="color: var(--color-forest-800)">Kai Kanesaka</span>
    </a>
    <div class="flex gap-6 text-sm" style="color: var(--color-forest-400)">
      <a href="#about" class="hover:text-emerald-600 transition-colors font-mono">// about</a>
      <a href="#works" class="hover:text-emerald-600 transition-colors font-mono">// works</a>
      <a href="#timeline" class="hover:text-emerald-600 transition-colors font-mono">// story</a>
      <a href="#contact" class="hover:text-emerald-600 transition-colors font-mono">// contact</a>
    </div>
  </div>
</nav>
